"""preflight command stub — replaced by Task 12."""

from __future__ import annotations

import argparse
import sys


def run(ns: argparse.Namespace, argv_rest: list[str]) -> int:
    print("preflight: not yet implemented", file=sys.stderr)
    return 1
